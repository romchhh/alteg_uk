import { Product, ProductCategory, ProductCategoryInfo } from '@/lib/types/product';

// Re-export from catalog for backward compatibility
export { PRODUCT_CATEGORIES, CATALOG_PRODUCTS as SAMPLE_PRODUCTS } from '@/lib/constants/catalog';

